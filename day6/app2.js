// JavaScript Template Strings

// Template Strings use back-ticks (``) rather than the quotes ("") to define a string:

let text = `Hello Word!`;
console.log(text);

let firstName = "Ganesh";
let lastName = "Regmi";

let Final = `hello i am ${firstName} ${lastName}`;
console.log(Final);
